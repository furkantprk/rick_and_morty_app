import 'package:flutter/material.dart';

class AppIcons {
  static const Icon characters = Icon(Icons.people);
  static const Icon locations = Icon(Icons.location_on);
  static const Icon following = Icon(Icons.favorite);
  static const Icon search = Icon(Icons.search);
  static const Icon followed = Icon(Icons.done);
  static const Icon unfollowed = Icon(Icons.add);
  static const Icon rename = Icon(Icons.drive_file_rename_outline_rounded);
}
